server:
  port: 8767
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/garenwen/spring-cloud-frame/     # 配置git仓库的地址
          search-paths: config-repo                             # git仓库地址下的相对地址，可以配置多个，用,分割。
          username: wenmxiang@gmail.com                         # git仓库的账号
          password: Java@0504                                   # git仓库的密码